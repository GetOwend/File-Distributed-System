# File-Distributed-System

pip install -r requirements.txt (environment optional)

python run.py

View available endpoints by visiting localhost:8000/docs

Use PostMan to call API Endpoints

Steps to login a user and get authentication token:

1. Register a user
2. Login your user
3. Grab the access token login returns because it is needed in all other endpoint calls
4. starting calling other endpoints